// ABOUTME: PostCSS configuration for Tailwind CSS v4.
// ABOUTME: Uses the @tailwindcss/postcss plugin.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
